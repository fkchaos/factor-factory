# HANDOFF 历史归档 · 2026-08-06

> **本文件为活文档 `docs/HANDOFF.md` 的历史快照归档**，按 HANDOFF.md 维护纪律「若需保留历史快照，另存 docs/HANDOFF-YYYYMMDD.md」生成。
> **为什么归档**：活文档只保留「最新状态 + 遗留待办 + 红线 + 环境 + 文件索引 + 交接说明」。以下内容均为**已完成流水账 / 历史阶段总结 / 已被最新快照取代的旧下一步**，不再出现在活文档中，但保留于此供追溯。
> **已完成项的去向**：
> - **因子级交付**（f0001a / f0002a …）→ 自动体现在 `docs/factor_board.html`（经 `research/_REGISTRY.csv`，由驱动器 cron 刷新）
> - **项目阶段完成**（特征工厂 / 组合构建 / 数据源 / 监控 / 审计等基础设施）→ 本归档 + `README.md` / `ARCHITECTURE.md` 的进度段
> - **当前真实待办** → 看活文档「🆕 最新快照 · 下一步待办」

---

## 一、本轮增量三轮（R2026-0804 晚 ~ R2026-0805 下午）

### 本轮增量（R2026-0805 下午 · 因子质量攻坚，见 RESEARCH_LOG R2026-0805-02,03）
- ✅ **overnight 因子符号翻转**：拆解实证（baostock sz50 + Tushare SZ300 双源交叉）——overnight 成分正信号(+0.024)、intraday 成分日内反转(-0.028)；旧方向 `-(overnight-intraday)` RankIC -0.040 方向反；**翻转后 `(overnight-intraday)` RankIC +0.040 / ICIR 11.6**。factors/overnight_intraday.py 已翻转，docstring 同步。⚠️ 1 日反转换手极高，成本敏感。
- ✅ **DSR/PBO 过拟合审计落地**（validate/overfit_audit.py + validator 接入）：DSR（Bailey & LdP 2014）+ PBO（Bailey 2015 CSCV）按论文自研（backtest-audit 是代码审计器非统计器、purgedcv 接口面向 ML，实测后决策自研）；n_trials=8（诚实估计含翻转前方向）；候选集=[主因子, 等权, 1日反转, 1日动量]，S=12 块 924 组合；门禁 DSR≥0.95 & PBO≤0.30。sz50 两因子 DSR=1.0/PBO=0.0。
- ✅ **行业/市值中性化**：validator 预处理链路 = MAD → 中性化(回归残差) → Z-score。BaoStockProvider 新增 get_industries()（query_stock_industry 证监会分类）；市值用 AkShare 快照。实证：ivol ICIR 2.35→3.09（剥离小市值暴露后更稳），IC 略降属正常（风格暴露剥离）。
- ✅ **测试**：新增 15 项（test_overfit_audit 12 + test_neutralize 3），全量 pytest **82 项全绿**。
- ✅ **最终流水线**（baostock sz50 2024 起，中性化开）：overnight RankIC +0.017、ivol +0.018（均 DSR=1.0/PBO=0.0）；组合 **+25.6% / Sharpe 1.01 / MaxDD -15.1%**。

### 本轮增量（R2026-0805 上午 · BaoStock 免积分数据源，见 docs/PLAN_BAOSTOCK_PROVIDER.md + RESEARCH_LOG R2026-0805-01）
- ✅ **`BaoStockProvider`（data/providers.py）**：移植用户旧工程 a-share-quant-sim 取数逻辑（约 60-70% 复用），适配本项目契约——volume=**股**、adjustflag=**2 前复权**、代码 `600000.SH`、parquet 缓存（.cache/baostock/）、`history_start` 窗口裁剪。**免 token / 免积分，Tushare 积分卡脖子的三项能力（前复权/换手率/指数池）全解锁**。
- ✅ **契约扩列**：`contract.py` PANEL_FIELDS 新增 `tradestatus`(0停牌/1正常) / `is_st`(0正常/1ST)（baostock 独有，其他源缺省 NaN）；`normalize_code` 支持 `sh.600000` 点分隔前缀（baostock 格式）。
- ✅ **pandas2 兼容坑绕行**：baostock 0.9.30 `rs.get_data()` 内部用 `df.append`（pandas≥2.0 已移除）多页必崩 → `_collect_rows` 手动 `next()/get_row_data()` 循环，零 monkey-patch。
- ✅ **市值方案**：baostock `query_stock_basic` 实测无股本字段 → 复用 AkShare 东财 spot 总市值快照（.cache/baostock/market_cap.csv，口径与 AkShareProvider 一致，失败降级 NaN）。
- ✅ **FF_PROVIDER 切换**：`scripts/real_research.py` 支持 `FF_PROVIDER=tushare|akshare|baostock`（默认 tushare）；baostock 免 token。`scripts/cross_source_check.py --source baostock` 支持免积分跨源核对。
- ✅ **验证**：sz50 全池 smoke 市值量级正确（中石化 6034 亿/中信 4188 亿/三一 1788 亿）；**67 项 pytest 全绿**（原 46 + 新 21 test_providers_baostock.py）；真实流水线 `FF_PROVIDER=baostock FF_UNIVERSE=sz50 FF_START=2024-01-01` **1m45s 跑通**（n_days=374，组合 +21.3%/Sharpe 0.70/MaxDD -20%）。
- ✅ **跨源信号一致性实证**：baostock 数据跑出因子方向与 Tushare 完全一致——ivol RankIC +0.030（Tushare +0.054，同为低波正信号）、overnight -0.033（Tushare -0.037，同为负信号）。**数据源切换不改变因子结论，契约防火墙实证生效**。
- ⚠️ **baostock 已知限制**：当日数据收盘后才更新（适合历史回测）；免费服务偶发不稳（health_check + 缓存兜底）；Windows 退出偶发崩溃 0xC0000409（不显式 logout，进程退出自然断开）；universe 仅支持指数池/ALL（无交易所子集）。
- ⚠️ **遗留小事**：`.cache/baostock/` 下残留旧格式 `sh.*.parquet`（首次失败缓存，不会被读到，可手动清理；沙箱回收站不可用导致 rm/unlink 会报错，用正常 shell 清理）。

### 本轮增量（R2026-0804 晚 · 数据契约 + 限频攻坚）
- ✅ **数据契约层**：`data/contract.py`（PANEL_FIELDS 单位表 / normalize_code / canonicalize_panel / validate_panel / validate_returns）；`data/interface.py` Protocol 补 `get_index_returns` + 契约 docstring；`docs/DATA_CONTRACT.md`；ARCHITECTURE §3.1。**全量 46 项 pytest 通过**（27 旧 + 19 新 `test_provider_contract.py`）。
- ✅ **修 3 个真 bug（探针抓出，解释了 SZ 重测前两次 n=0）**：
  1. `self._pro.pro_bar` 不存在（pro_bar 是 tushare **模块级函数**）→ 改 `self._ts.pro_bar`；
  2. `daily_basic` 免费档限频 **1次/分钟** → `_daily_basic_broken` 熔断闩（首次超限整轮跳过，turnover/market_cap=NaN）；
  3. `stock_basic` 免费档限频 **1次/小时** → 空壳缓存陷阱（空 CSV 被当有效池）已修：空壳视为无效重拉 + 空池直接抛错。
- ✅ **Tushare 免费档价格降级为不复权(raw)**：`adj_factor` 限频 1次/小时，pro_bar(adj=qfq) 拉不了全市场；已去 adj 参数。升级积分后可改回（一行）。
- ✅ **AkShare Provider 合规修复**：代码补后缀（normalize_code）、volume 手→股 ×100、market_cap 改 spot 快照（元）替代 0 占位、索引 date/asset、返回前 canonicalize+validate。
- ✅ **`scripts/make_universe.py`**：用 AkShare 生成股票池 CSV 落 `.cache/tushare/`（绕开 stock_basic 限频），格式与 `_asset_list` 缓存一致。
- ✅ **跨源一致性核对实证**：Tushare(raw) vs AkShare 新浪源(raw)，000001/000002.SZ 各 117 共同日，close/volume/amount 最大相对差 **0.0000%**——契约在真实源间生效。核对脚本 `scripts/cross_source_check.py`（注意其 get_panel 会拉全池，核对时用 _fetch_one 内联更省）。
- ✅ **AkShare 双源降级**：`_hist_df` 主源 `stock_zh_a_hist`(东财, volume=手→×100) 断连时降级 `stock_zh_a_daily`(新浪, volume=股不乘, 无 turnover)；`_market_cap_map` 失败降级空映射(NaN 合规)。**本环境东财端点不可达（RemoteDisconnected），新浪可通。**
- ✅ **依赖补装**：pyarrow 25.0.0（pandas3.x parquet 必需，漏装重测必"秒死"）；akshare 1.18.81（pip 安装须 `--no-cache-dir` 绕沙箱缓存清理）。
- ✅ **SZ 真实重测首轮有效结果（历史性）**：单因子（2020 起 SZ300 raw价，约1600交易日）——**ivol RankIC=+0.0537 / ICIR=+0.3818 / 胜率67% / 衰减20d=+0.109**（低波异象真实站住）；overnight_intraday RankIC=-0.0374 / ICIR=-0.2717（负信号待复核符号约定）。组合回测（2023 起 top_n=10 ICIR加权 含二次冲击成本）：**total_return=+24.2% / Sharpe=0.425 / MaxDD=-22.2%**。已写 TEST_LOG + 两张因子卡。
- ✅ **性能修复**：Tushare/AkShare Provider 加 `_panel_cache` 全历史内存缓存（validator 每日期重读 parquet 300 文件/次会卡死——这是此前 30 分钟无输出 + OOM 的根因）；`history_start` 窗口参数控内存（组合阶段全历史全池在沙箱会 OOM 无痕杀，建议 FF_START=2023 + top_n=10）。
- 🔄 **进行中**：待用户确认符号复核（overnight 因子）、DSR/PBO �审计、全池重测（可在非沙箱/更大内存环境跑全历史）。
- ⚠️ **沙箱内存红线**：全脚本 `real_research.py` 内组合阶段（combine+WalkForward）在沙箱**进程级无痕杀**（SIGKILL，try/except 拦不住）；单因子阶段稳定（2020/2023 窗口结果一致）。**要组合回测请单独进程跑**：`FF_UNIVERSE=SZ FF_START=2023-01-01 python -c "...top_n=10..."`（见今日记忆/日志中的探针代码），或换更大内存环境跑全历史全池。

---

## 二、Phase 1/2/3 阶段完成总结

### 已完成（Phase 1 地基）
- ✅ 外部调研：`docs/RESEARCH_LOG.md`（Qlib / Alphalens / Tushare / 预处理四层 + PIT / CSCV·DSR·PBO）。
- ✅ 架构：`docs/ARCHITECTURE.md`（六层解耦；前视防护在接口层强制）。
- ✅ 决策记录：`docs/adr/0001-layered-decoupled-architecture.md`。
- ✅ 核心接口契约：`data/interface.py`、`factors/interface.py`(含 `assert_no_lookahead`)、`engine/interface.py`。
- ✅ 测试脚手架：`tests/conftest.py`、`tests/test_lookahead.py`（4 项全过）。
- ✅ 文档模板 + README。

### 已完成（Phase 2 四件套 + 2 因子 + 组合合成）
- ✅ 数据适配器：`data/providers.py`（LocalProvider 确定性合成源；TushareProvider 主 / AkShareProvider fallback **真实实现**）。
- ✅ 因子 1：`factors/overnight_intraday.py`（隔夜-日内拆解）；计划 `fp_overnight_intraday.md`。
- ✅ 因子 2：`factors/ivol.py`（特质波动率=回归残差波动，反向做多低波动）；计划 `fp_ivol.md`。
- ✅ 引擎：`engine/engine_impl.py`（WalkForward + T+1 开盘 + 二次冲击成本 + PIT 池 + 涨跌停近似）。
- ✅ 验证器：`validate/validator.py`（RankIC/ICIR/衰减/分层 + DSR/PBO 预留）。
- ✅ 组合合成：`portfolio/combiner.py`（CompositeFactor 接入引擎；等权/ICIR 加权；simple_orthogonalize 去冗余）。
- 📌 实测（合成随机数据 seed=7，**仅验证框架、非真实 alpha**）：
  - overnight_intraday：RankIC≈-0.003；回测 Sharpe 0.67。
  - ivol：RankIC≈0.0002 / ICIR≈0.001；回测 Sharpe -0.21。
  - 组合（ICIR 加权）：回测 Sharpe 0.78；权重 overnight 0.936 / ivol 0.064。**真实数据接入后必须重测。**

### 已完成（Phase 3：监控 + 真实数据接入准备 + 影子账户方案 A）
- ✅ 监控看板：`monitor/monitor.py`（FactorMonitor：分布漂移 / IC 衰减 / 拥挤度 HHI / 组合归因骨架）；`tests/test_monitor.py`(6 项)。
- ✅ **真实数据接入 SOP**：`docs/SOP_TUSHARE_TOKEN.md`。
- ✅ **TushareProvider / AkShareProvider 真实实现**（`data/providers.py`）：lazy import、本地缓存、单位统一、get_index_returns、list_universe 近似；修复 get_panel(None) 与 _fetch_one 缓存一致性。
- ✅ **tushare 已装**：托管 venv 装好 `tushare==1.4.29`。
- ✅ **股票池子集支持（修复 `FF_UNIVERSE` bug）**：`TushareProvider._asset_list` 现支持三种模式——指数成分股(`hs300/csi500/zz500/sz50/cyb/zxb`，走 `index_weight`)、交易所(`SZ/SH/BJ`，走 `stock_basic exchange`)、list_status(`L/D/P`)；不同模式独立缓存文件，避免污染。`tests/test_providers_subset.py`(4 项) 覆盖。
- ✅ **token 文件化配置**：`configs/tushare.yaml` 模板已建（被 .gitignore 忽略）；`scripts/real_research.py::_load_token` 支持读该文件。原因：WorkBuddy Bash 子进程**读不到**用户在交互式 shell 配的环境变量 `TUSHARE_TOKEN`（已实测 `ENV present=False`），文件方式最稳。
- ✅ **一键重测脚本**：`scripts/real_research.py`。
- ✅ **因子冗余/正交化检查**：`validate/redundancy.py` + `tests/test_redundancy.py`(2 项)。
- ✅ **共享选股层（解耦 XDT）**：`engine/selection.py`，WalkForwardEngine 与 ShadowAccount 共用。
- ✅ **影子账户方案 A（纯回测影子，不接券商）**：`portfolio/shadow_account.py` + `ShadowConfig`/`ShadowResult` + `tests/test_shadow.py`(3 项) + 设计文档。
- ✅ **引擎重构**：WalkForwardEngine.run 改用共享选股层。
- 📌 实测（合成随机数据 seed=7）：影子账户 total_return -9.1% / Sharpe -0.094 / MaxDD -30.0% / 121 次调仓 / 平均单边换手 70.8%（随机噪声，非真实参考）；benchmark NaN（LocalProvider 无指数基准）。**真实数据接入后必须重测。**
- 📌 **全量 27 项 pytest 通过**（前视 4 + 因子/引擎 5 + 组合 3 + 监控 6 + 冗余 2 + 影子 3 + 子集 4）；`py_compile` 语法检查通过。

### 阻塞 / 待用户决策（大问题才找用户）
- ⚠️ **真实 token 待填充**：代码、库、子集支持全部就绪。用户在交互式 shell 配的 `TUSHARE_TOKEN` 环境变量在本 Bash 环境读不到；已改用文件方式，请用户把真实 token 填入 `configs/tushare.yaml` 的 `token:` 字段（替换占位 `请在此填入你的Tushare token`）。填好后主理即可跑 `FF_UNIVERSE=hs300 python scripts/real_research.py` 重测真实 alpha。
- ⚠️ **影子账户方案 B（接模拟券商 QMT/PTrade）**：用户明确"外部环境啥时候有了之后再说"——方案 A 已落地，方案 B 暂缓。

---

## 三、旧下一步工作（已被 2026-08-06 晚「最新快照 · 下一步待办」取代）

### 下一步工作（按优先级，2026-08-05 视角）
- **P0（真实重测，进行中）**：① 等 akshare 装完（`--no-cache-dir` 重装，task 6AczoF）→ 跑 `python scripts/make_universe.py SZ` 生成股票池 CSV；② 重跑 `FF_UNIVERSE=SZ python scripts/real_research.py`（calls_per_min=200，日志 .cache/real_research.log；2800 只 × raw 日线 ≈ 1-2 小时，parquet 缓存可续跑）；③ 产出真实 RankIC 后做 Tushare(raw) vs AkShare(qfq) 跨源一致性核对（同票同日 close/volume/amount，注意复权口径差异）。**不要直接用 hs300**（index_weight 需 500 积分）。
- **P1**：DSR / PBO 过拟合审计落地（validator 接口已预留）。
- **P1**：月度评审机制 + 组合月报。
- **P2**：Phase 4 因子扩展（价值 EP/BP、质量 ROE、SUE/PEAD、动量脉冲）+ point-in-time 财报对齐。
- **P2（待外部券商环境）**：影子账户方案 B。
