# 双过滤动量（f0052a）

- f-code：f0052a
- 类别 / 方向：反向（因子值越大，未来收益越低）
- 逻辑一句话：双过滤动量：12-1 相对动量，叠加 12 月绝对收益方向门控。
- 是否组合：否
## 分池 IC（全样本，决策5：覆盖 2 池）
| 池 | RankIC | ICIR | IC胜率 | 样本日 |
|---|---|---|---|---|
| hs300 | -0.0057 | -0.055 | 48.8% | 1343 |
| hs800 | -0.0094 | -0.097 | 45.6% | 1343 |

> 注：RankIC 量级偏低属常态（A股技术因子半衰期~18个月，见 v75 复盘）。**质量高低由策略组选择，内部不设 |IC|≥0.03 出库门槛**（用户决策4）。

## 主池验证指标（池：hs300，窗口 2020-01-01 起）
| 指标 | 值 | 说明 |
|---|---|---|
| RankIC | -0.0057 | 参考，非门槛 |
| ICIR | -0.055 | 参考，非门槛 |
| IC 胜率 | 48.8% | 参考，非门槛 |
| 年化收益(净) | 23.50% | >0 即正向 |
| Sharpe(净) | 0.83 | 参考 |
| 最大回撤(净) | -38.91% | 关注成本敏感 |
| 年化收益(毛) | 26.59% | 参考 |
| DSR | 1.000 | **过拟合门禁** ≥0.95 |
| PBO | — | **过拟合门禁** <0.25 |
| 审计结论 | PASS | PASS=未过拟合 |

> 交易成本假设：单边 0.10%（按 top20 名单变动比例计换手）。
> 毛/净差距大 = 该因子换手高、成本敏感，落地前务必核算真实费率。
> DSR/PBO 是**过拟合审计**（防数据窥探），与"因子质量高低"是两回事；质量筛选交策略组。

## 框架一致性字段
- 中性化状态：industry+mktcap(PIT: amount/turnover 现算流通市值)
- PIT 认证：true
- 基准池：hs300（**口径锚点，非最优池**——只是 manifest 声明的第一个池，用于统一顶层字段口径）
- ⚠️ 本厂不指定「主场池」：按全样本 IC/ICIR 挑池属**后视选池**（与用未来市值选股同类），会虚高回测；配池请看上方分池 IC 表自行判断。
## 消费指引
- 因子值已中性化，可直接 load 进选股模型。
- 适用池子：hs300, hs800；不适用：见换池反转预警。
- 成本敏感性：净 alpha 对换手敏感，低换手场景优先。
- 冗余关系：见 correlation.csv（ρ≥0.7 勿重复入模）；该矩阵在 **hs300** 单池上计算，非六池平均，换池后相关性可能变化。
- 复现命令：FF_PROVIDER=baostock python scripts/real_research.py --factor dual_filter_momentum --pool hs300

## 聚合视图（本因子在聚合交付中的位置）
- 机器可读发货形态：`../strategy_export/stock_factors.json`（条目 name=f0052a，供策略组阶段 0 消费）
- 跨池检验记录：`../universe_matrix/ic_matrix_<最新日期>.csv` 及同批 icir / dsr 三表（本因子行以矩阵实际收录为准）
- 说明：上述为同一交付物的聚合 / 检验视图，由 `scripts/export_to_strategy_json.py` / `scripts/factor_universe_matrix.py` 生成，与本卡同源，不另立交付编号。
- 已知陷阱：特定牛熊阶段 / 流动性枯竭。

