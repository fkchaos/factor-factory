# 因子计划文档模板（Plan-First 门禁）

> 每个新因子/新阶段**写码前**必须填此模板，评审通过才动手（禁止"先写再想"）。
> 文件命名：`research/plans/YYYYMMDD-<factor_name>.md`

## 1. 基本信息
- 因子名（唯一 ID）：
- 作者 / 日期：
- 所属类别（价值/规模/动量反转/质量/成长/低波动/流动性/微观结构/资金流/事件）：

## 2. 假设与逻辑（先有逻辑，再验证）
- 经济学/行为金融逻辑：
- 为什么在 A 股日频场景应有效：
- 外部调研依据（引用 RESEARCH_LOG 编号，如 R2026-0804-02）：
- 是否纯数据挖掘（若是，需额外正交化 + 多重检验证据）：

## 3. 方法与参数（参数冻结）
- 输入字段：
- 计算公式（伪代码/表达式）：
- 预处理：MAD 去极值 n= / Z-score / 中性化（行业+市值）：
- 候选参数（冻结值，评估后改参视为新假设）：
- 排序方向（高=多头 / 低=多头）：

## 4. 验证标准（门禁）
- Rank IC ≥ ：
- |ICIR| ≥ ：
- 分层单调性：要求单调 / 不要求：
- t 统计量 ≥ ：
- 成本后净夏普 > 0：
- DSR p-value > 0.95 / PBO < 0.25（样本外确认阶段）：

## 5. 失败如何判定（明确红线）
- 若 IC 不显著 -> 淘汰，记 TEST_LOG（失败）。
- 若正交化后 IC 塌缩（是风格马甲）-> 淘汰。
- 若 DSR/PBO 不达标 -> 降级为"观察"，不入生产。
- 若前视审计（assert_no_lookahead）失败 -> 禁止合并，立即修。

## 6. 数据需求
- 数据源（Tushare/AkShare/Local）：
- 是否需要 point-in-time 财报：
- 频率（日/分钟）：

## 7. 评审记录
- 评审人 / 日期 / 通过 or 打回：
